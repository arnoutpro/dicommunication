# Desktop-only extras for the Windows MSI and macOS DMG (native app window).
# Docker and `make run` do not need these.
pywebview>=5.4,<6
pythonnet>=3.0,<4; sys_platform == "win32"
pyobjc-core>=10; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10; sys_platform == "darwin"
pyobjc-framework-WebKit>=10; sys_platform == "darwin"
